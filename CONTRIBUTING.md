# Contributing

Build and validate changes on Windows:

```powershell
.\scripts\local-check.ps1
```

Keep provider credentials and local session contents out of commits, logs, test
fixtures, screenshots, and issue reports. UI changes should include a fresh
native build and Windows interaction proof.
